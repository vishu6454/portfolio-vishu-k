# Git Migration to New Repo TODO

## Pending Steps:
[x] 1. Initialize Git repository (`git init`)
- [ ] 2. Add all files (`git add .`)
- [ ] 3. Initial commit (`git commit -m "Initial commit"`)
- [ ] 4. Add remote origin (`git remote add origin https://github.com/vishu6454/portfolio-vishu-k.git`)
- [ ] 5. Rename branch to main and push (`git branch -M main && git push -u origin main`)
- [ ] 6. Verify push
