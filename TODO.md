# Git Migration to New Repo TODO

## Completed Steps:
[x] 1. Initialize Git repository (`git init`)
[x] 2. Add all files (`git add .`)
[x] 3. Initial commit (`git commit -m \"Initial commit: Portfolio with React frontend and Node.js backend\"`)
[x] 4. Add remote origin (`git remote add origin https://github.com/vishu6454/portfolio-vishu-k.git`)
[x] 5a. Rename branch to main (`git branch -M main`)

## Pending Steps:
- [ ] 5b. Resolve remote conflict and push (`git pull origin main --allow-unrelated-histories && git push -u origin main`)
- [ ] 6. Verify push (`git log --oneline -5 && echo 'Check https://github.com/vishu6454/portfolio-vishu-k.git'`)
